<Window x:Class="FlexiTrack.Desktop.Views.TrayPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:FlexiTrack.Desktop.Views"
        Title="FlexiTrack"
        Width="360"
        Height="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Deactivated="Window_Deactivated">

    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{StaticResource PrimaryBrush}"
                    CornerRadius="8,8,0,0"
                    Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="FlexiTrack"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="White" />
                        <TextBlock x:Name="UserNameText"
                                   FontSize="12"
                                   Foreground="#CCFFFFFF" />
                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="X"
                            Click="CloseButton_Click"
                            Width="24"
                            Height="24"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="Bold"
                            Cursor="Hand" />
                </Grid>
            </Border>

            <!-- Tab Buttons -->
            <Grid Grid.Row="1" Margin="16,12,16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <RadioButton Grid.Column="0"
                             x:Name="LogTab"
                             Content="Log"
                             IsChecked="True"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked" />

                <RadioButton Grid.Column="1"
                             x:Name="SummaryTab"
                             Content="Summary"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked" />

                <RadioButton Grid.Column="2"
                             x:Name="ExportTab"
                             Content="Export"
                             Style="{StaticResource TabButtonStyle}"
                             Checked="Tab_Checked" />
            </Grid>

            <!-- Content Area -->
            <ScrollViewer Grid.Row="3"
                          Margin="16,12,16,16"
                          VerticalScrollBarVisibility="Auto">
                <Grid>
                    <!-- Quick Log View -->
                    <views:QuickLogView x:Name="LogContent"
                                        Visibility="Visible" />

                    <!-- Summary View -->
                    <views:SummaryView x:Name="SummaryContent"
                                       Visibility="Collapsed" />

                    <!-- Export View -->
                    <views:ExportView x:Name="ExportContent"
                                      Visibility="Collapsed" />
                </Grid>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
