<Window x:Class="FlexiTrack.Desktop.Views.EditTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Task"
        Width="380"
        Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Edit Task"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}"
                   Margin="0,0,0,16" />

        <!-- Date -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Date" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
            <DatePicker x:Name="DatePicker" />
        </StackPanel>

        <!-- Time Range -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Time" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" x:Name="StartTimeBox" />
                <TextBlock Grid.Column="1" Text=" - " VerticalAlignment="Center" Margin="8,0" />
                <TextBox Grid.Column="2" x:Name="EndTimeBox" />
            </Grid>
        </StackPanel>

        <!-- Description -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Description" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
            <TextBox x:Name="DescriptionBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Height="60"
                     VerticalContentAlignment="Top" />
        </StackPanel>

        <!-- Client -->
        <StackPanel Grid.Row="4" Margin="0,0,0,12">
            <TextBlock Text="Client (optional)" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
            <ComboBox x:Name="ClientCombo" IsEditable="True" />
        </StackPanel>

        <!-- Error Message -->
        <TextBlock Grid.Row="5"
                   x:Name="ErrorText"
                   Foreground="{StaticResource DangerBrush}"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Visibility="Collapsed" />

        <!-- Buttons -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Click="Cancel_Click"
                    Style="{StaticResource SecondaryButton}"
                    Width="90"
                    Margin="0,0,12,0" />
            <Button Content="Save"
                    Click="Save_Click"
                    Style="{StaticResource PrimaryButton}"
                    Width="90" />
        </StackPanel>
    </Grid>
</Window>
